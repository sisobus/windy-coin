[workspace]
resolver = "2"
members = ["host", "methods"]
exclude = ["guest"]

# Guest builds slowly without optimization, so always optimize.
[profile.dev]
opt-level = 3

[profile.release]
debug = 1
lto = true
